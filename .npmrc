# Monorepo-wide npm configuration.
# legacy-peer-deps is required because the React Native ecosystem contains
# packages whose peer dependency declarations do not satisfy npm's strict
# resolution algorithm. Removing this flag will cause install failures.
legacy-peer-deps=true
