# Expo workspace npm configuration.
# prefer-workspace-packages ensures npm resolves React Native packages
# from this workspace's local node_modules rather than the hoisted root,
# preventing the duplicate-module instance bug at runtime.
legacy-peer-deps=true
prefer-workspace-packages=true
